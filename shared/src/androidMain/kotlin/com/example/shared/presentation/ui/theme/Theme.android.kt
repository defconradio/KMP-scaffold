package com.example.shared.presentation.ui.theme

import androidx.compose.runtime.Composable

@Composable
actual fun SystemBarColoring() {
    // Android-specific system bar coloring is handled by the platform-specific AppTheme or Activity
    // However, if we want to handle it here, we would need to depend on platform libraries.
    // For now, we leave this empty as per reference design pattern for pure common code
}
