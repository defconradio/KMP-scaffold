// Simple JS entrypoint that demonstrates requiring the shared KMP JS artifact if published locally
console.log('Aguacate KMP JS entrypoint');

// If the KMP JS artifact is built and available under `shared/build/...`, you can require/import it here.
// Example (adjust path after building):
// const shared = require('./shared/build/js/packages/aguacate-kmp/kotlin/aguacate-kmp.js');
// console.log(shared.com.example.shared.Greeting().greeting());
